{
  "name": "@threadvm/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "threadvm": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@threadvm/server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
